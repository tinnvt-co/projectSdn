# Landing Page

- [/] Create `LandingPage.jsx` with carousel + info sections
- [ ] Create `LandingPage.css` with FPT-style design
- [ ] Add route `/` -> `LandingPage` in `App.jsx` (before redirect to /login)
